# Required repository secrets:
#   CURSOR_API_KEY      — Cursor Cloud Agents API key (Integrations in Cursor dashboard).
#   MAERSK_SCHEMAS_PAT — Fine-grained GitHub PAT with Contents: Read on
#                        Maersk-Global/API-JSON-Schema-Definitions and Maersk-Global/ui-myfinance.
# Add with: gh secret set CURSOR_API_KEY --repo <owner/repo>
#           gh secret set MAERSK_SCHEMAS_PAT --repo <owner/repo>
#
# Verify Cursor GitHub App can access ui-myfinance (Cloud Agents need this; PAT alone is not enough):
#   curl -sS https://api.cursor.com/v1/repositories -u "$CURSOR_API_KEY:" \
#     | jq '.items[] | select(.url | contains("ui-myfinance"))'
#
# For auto-created PRs, the Cursor GitHub App must also be allowed to push a branch and open
# pull requests on Maersk-Global/ui-myfinance (not read-only).
#
# The Cloud Agent must use repos[].url = ui-myfinance. If you pass github.repository here,
# the agent runs against this automation repo and any PR opens there — not in ui-myfinance.
#
# PR creation: autoCreatePR opens a GitHub PR only after the Cloud Agent run reaches FINISHED.
# If the run ends in ERROR (wrong startingRef, push denied, etc.), no PR appears — check the agent URL.

name: Sync myfinance schemas (Cursor agent)

on:
  workflow_dispatch:
    inputs:
      only:
        description: "Optional regex to limit scope (e.g. 'invoices'). Matched against local schema filenames. Empty = all tracked files."
        required: false
        default: ""
      dry_run:
        description: "If true, the agent only reports what it would change."
        type: boolean
        default: false
      model:
        description: "Cursor model id (leave empty for default)."
        required: false
        default: ""
      ui_myfinance_default_branch:
        description: "repos[0].startingRef for ui-myfinance. Leave empty to resolve the repo default branch via the GitHub API."
        required: false
        default: ""
      wait_for_agent_run:
        description: "Poll until the Cursor run finishes. PR is created only after status FINISHED; enable this to catch ERROR runs before the job exits."
        type: boolean
        default: true
      agent_poll_timeout_minutes:
        description: "Maximum minutes to poll when wait_for_agent_run is true."
        required: false
        default: "60"

jobs:
  # ──────────────────────────────────────────────────────────────────────
  # Job 1: Deterministic pre-flight gate.
  # Clones API-JSON-Schema-Definitions (org-private) and sparse-checks out
  # ui-myfinance/schemas/, diffs tracked YAMLs, and emits a JSON change list.
  # If nothing drifted the workflow ends here — zero Cursor tokens spent.
  # ──────────────────────────────────────────────────────────────────────
  detect-drift:
    name: Detect schema drift (no agent)
    runs-on: ubuntu-latest
    outputs:
      has_changes: ${{ steps.diff.outputs.has_changes }}
      changed_files: ${{ steps.diff.outputs.changed_files }}
      source_sha: ${{ steps.diff.outputs.source_sha }}
    steps:
      - uses: actions/checkout@v4

      - name: Clone API schema source (org-private)
        env:
          GH_TOKEN: ${{ secrets.MAERSK_SCHEMAS_PAT }}
        run: |
          git clone --depth=1 \
            "https://x-access-token:${GH_TOKEN}@github.com/Maersk-Global/API-JSON-Schema-Definitions" \
            /tmp/api-src

      - name: Sparse-checkout ui-myfinance/schemas
        env:
          GH_TOKEN: ${{ secrets.MAERSK_SCHEMAS_PAT }}
        run: |
          git clone --depth=1 --filter=blob:none --sparse \
            "https://x-access-token:${GH_TOKEN}@github.com/Maersk-Global/ui-myfinance" \
            /tmp/ui-src
          git -C /tmp/ui-src sparse-checkout set schemas

      - name: Compute drift
        id: diff
        env:
          ONLY: ${{ inputs.only }}
        run: |
          set -euo pipefail
          SRC=/tmp/api-src
          UI_SRC=/tmp/ui-src
          SOURCE_SHA=$(git -C "$SRC" rev-parse HEAD)

          # ── Mapping table ──────────────────────────────────────────
          # Each line: <remote path relative to repo root> <TAB> <local filename in schemas/>
          # The public repo uses different directory prefixes and occasionally
          # a "35-" filename prefix that the local copy strips.
          # Only files we actually run codegen for are tracked.
          MAPPING=$(cat <<'MAP'
          apis/500-Maersk.com/v1/myfinance-invoices-API.v1.yml	myfinance-invoices-API.v1.yml
          apis/500-Maersk.com/v2/myfinance-invoices-API.v2.yaml	myfinance-invoices-API.v2.yaml
          apis/500-Maersk.com/v1/myfinance-submit-proof-of-payment-API.v1.yaml	myfinance-submit-proof-of-payment-API.v1.yaml
          apis/500-Maersk.com/v1/35-myfinance-export-documents-API.v1.yml	myfinance-export-documents-API.v1.yml
          apis/500-Maersk.com/v1/myfinance-refund-request-API.v1.yaml	myfinance-refund-request-API.v1.yaml
          apis/500-Maersk.com/v1/myfinance-estatements-API.v1.yml	myfinance-estatements-API.v1.yml
          apis/500-Maersk.com/v1/myfinance-workflows-API.v1.yaml	myfinance-workflows-API.v1.yaml
          MAP
          )

          CHANGES='[]'

          while IFS=$'\t' read -r remote_rel local_name; do
            # Trim leading whitespace from heredoc indentation
            remote_rel=$(echo "$remote_rel" | xargs)
            local_name=$(echo "$local_name" | xargs)

            # Honour the ONLY filter (matched against local filename)
            if [ -n "$ONLY" ] && ! echo "$local_name" | grep -Eq "$ONLY"; then
              continue
            fi

            remote_path="$SRC/$remote_rel"
            local_path="$UI_SRC/schemas/$local_name"

            if [ ! -f "$remote_path" ]; then
              # Remote file was removed — local schema should be deleted
              if [ -f "$local_path" ]; then
                CHANGES=$(echo "$CHANGES" | jq --arg r "$remote_rel" --arg l "$local_name" \
                  '. + [{"remote": $r, "local": $l, "status": "deleted"}]')
              fi
            elif [ ! -f "$local_path" ]; then
              # Remote file exists but local does not — new schema
              CHANGES=$(echo "$CHANGES" | jq --arg r "$remote_rel" --arg l "$local_name" \
                '. + [{"remote": $r, "local": $l, "status": "added"}]')
            elif ! diff -q "$remote_path" "$local_path" >/dev/null 2>&1; then
              # Both exist but content differs — modified
              CHANGES=$(echo "$CHANGES" | jq --arg r "$remote_rel" --arg l "$local_name" \
                '. + [{"remote": $r, "local": $l, "status": "modified"}]')
            fi
          done <<< "$MAPPING"

          TOTAL=$(echo "$CHANGES" | jq 'length')

          if [ "$TOTAL" -eq 0 ]; then
            echo "has_changes=false" >> "$GITHUB_OUTPUT"
            {
              echo "## No schema drift detected"
              echo ""
              echo "Source SHA: \`$SOURCE_SHA\`"
              echo ""
              echo "_The Cursor agent was NOT launched. Zero tokens spent._"
            } >> "$GITHUB_STEP_SUMMARY"
            exit 0
          fi

          echo "has_changes=true" >> "$GITHUB_OUTPUT"
          echo "source_sha=$SOURCE_SHA" >> "$GITHUB_OUTPUT"
          echo "changed_files=$(echo "$CHANGES" | jq -c '.')" >> "$GITHUB_OUTPUT"

          ADDED=$(echo "$CHANGES"   | jq '[.[] | select(.status == "added")]   | length')
          MODIFIED=$(echo "$CHANGES" | jq '[.[] | select(.status == "modified")] | length')
          DELETED=$(echo "$CHANGES"  | jq '[.[] | select(.status == "deleted")]  | length')

          {
            echo "## Schema drift detected"
            echo ""
            echo "Source SHA: \`$SOURCE_SHA\`"
            echo ""
            echo "**Added ($ADDED)**"
            echo "$CHANGES" | jq -r '.[] | select(.status == "added")   | "- `\(.local)` ← \(.remote)"'
            echo ""
            echo "**Modified ($MODIFIED)**"
            echo "$CHANGES" | jq -r '.[] | select(.status == "modified") | "- `\(.local)` ← \(.remote)"'
            echo ""
            echo "**Deleted ($DELETED)**"
            echo "$CHANGES" | jq -r '.[] | select(.status == "deleted")  | "- `\(.local)`"'
          } >> "$GITHUB_STEP_SUMMARY"

  # ──────────────────────────────────────────────────────────────────────
  # Job 2: Launch the Cursor Cloud Agent.
  # Only runs when detect-drift found at least one changed file.
  # Embeds the pre-computed change list and source SHA into the prompt
  # so the agent skips re-enumeration entirely.
  # ──────────────────────────────────────────────────────────────────────
  launch-agent:
    name: Launch Cursor agent
    runs-on: ubuntu-latest
    needs: detect-drift
    if: needs.detect-drift.outputs.has_changes == 'true'
    steps:
      - uses: actions/checkout@v4

      - name: Build prompt
        env:
          ONLY: ${{ inputs.only }}
          DRY_RUN: ${{ inputs.dry_run }}
          ACTOR: ${{ github.actor }}
          CHANGED_FILES: ${{ needs.detect-drift.outputs.changed_files }}
          SOURCE_SHA: ${{ needs.detect-drift.outputs.source_sha }}
        run: |
          set -euo pipefail
          PROMPT=$(envsubst < .cursor/prompts/sync-myfinance-schemas.md)
          # Write to a file — do not pass the prompt via GITHUB_OUTPUT or inline
          # workflow expressions into the next step's shell; JSON/markdown/newlines/braces
          # break the runner script if inlined.
          printf '%s\n' "$PROMPT" > /tmp/agent-prompt.txt

      - name: Launch Cursor agent
        env:
          CURSOR_API_KEY: ${{ secrets.CURSOR_API_KEY }}
          GH_TOKEN: ${{ secrets.MAERSK_SCHEMAS_PAT }}
          INPUT_UI_BRANCH: ${{ inputs.ui_myfinance_default_branch }}
          WAIT_FOR_RUN: ${{ inputs.wait_for_agent_run }}
          POLL_TIMEOUT_MIN: ${{ inputs.agent_poll_timeout_minutes }}
        run: |
          set -euo pipefail

          if [ -n "${INPUT_UI_BRANCH}" ]; then
            REF="$INPUT_UI_BRANCH"
          else
            REF=$(curl -sS -H "Authorization: Bearer ${GH_TOKEN}" \
              -H "Accept: application/vnd.github+json" \
              -H "X-GitHub-Api-Version: 2022-11-28" \
              "https://api.github.com/repos/Maersk-Global/ui-myfinance" \
              | jq -er .default_branch)
          fi
          echo "Using Cloud Agent startingRef: $REF"

          BODY=$(jq -n \
            --rawfile text /tmp/agent-prompt.txt \
            --arg repo "https://github.com/Maersk-Global/ui-myfinance" \
            --arg ref "$REF" \
            '{
              prompt: { text: $text },
              repos: [{ url: $repo, startingRef: $ref }],
              autoCreatePR: true,
              skipReviewerRequest: true
            }')

          if [ -n "${{ inputs.model }}" ]; then
            BODY=$(echo "$BODY" | jq --arg m "${{ inputs.model }}" '. + { model: { id: $m } }')
          fi

          HTTP_CODE=$(curl -sS -o /tmp/create-agent.json -w "%{http_code}" -X POST https://api.cursor.com/v1/agents \
            -u "${CURSOR_API_KEY}:" \
            -H 'Content-Type: application/json' \
            -d "$BODY")

          jq . /tmp/create-agent.json
          if [ "$HTTP_CODE" != "200" ] && [ "$HTTP_CODE" != "201" ]; then
            echo "::error::Cursor POST /v1/agents failed with HTTP $HTTP_CODE"
            exit 1
          fi

          AGENT_ID=$(jq -r '.agent.id' /tmp/create-agent.json)
          RUN_ID=$(jq -r '.run.id' /tmp/create-agent.json)
          AGENT_URL=$(jq -r '.agent.url // "unknown"' /tmp/create-agent.json)
          BRANCH=$(jq -r '.agent.branchName // "auto-generated"' /tmp/create-agent.json)

          FILE_COUNT=$(echo '${{ needs.detect-drift.outputs.changed_files }}' | jq 'length')

          POLL_TIMEOUT_MIN="${POLL_TIMEOUT_MIN:-60}"

          if [ "${WAIT_FOR_RUN}" = "true" ]; then
            DEADLINE=$(($(date +%s) + POLL_TIMEOUT_MIN * 60))
            while true; do
              NOW=$(date +%s)
              if [ "$NOW" -ge "$DEADLINE" ]; then
                echo "::error::Timed out after ${POLL_TIMEOUT_MIN} minutes waiting for Cursor run $RUN_ID"
                exit 1
              fi
              RUN_JSON=$(curl -sS -u "${CURSOR_API_KEY}:" \
                "https://api.cursor.com/v1/agents/${AGENT_ID}/runs/${RUN_ID}")
              STATUS=$(echo "$RUN_JSON" | jq -r .status)
              echo "Cursor run status: $STATUS"
              case "$STATUS" in
                FINISHED)
                  echo "Run FINISHED — if GitHub allows it, Cursor opens the PR after this point."
                  break
                  ;;
                ERROR|CANCELLED|EXPIRED)
                  echo "::error::Cursor run ended with status=$STATUS (no PR). Open the agent for logs: $AGENT_URL"
                  exit 1
                  ;;
                *)
                  sleep 30
                  ;;
              esac
            done
          else
            echo "Not polling run status — open $AGENT_URL and wait for FINISHED to see the PR on GitHub."
          fi

          {
            echo "### Cursor agent"
            echo "- Target repo (PR opens here): **Maersk-Global/ui-myfinance**"
            echo "- Agent: $AGENT_URL"
            echo "- Run: \`$RUN_ID\`"
            echo "- Branch: \`$BRANCH\` (from \`$REF\`)"
            echo "- Source SHA (API schemas): \`${{ needs.detect-drift.outputs.source_sha }}\`"
            echo "- Files passed in: $FILE_COUNT YAML(s)"
            echo ""
            echo "_PRs are created when the run completes successfully (FINISHED). If the run errors, check the agent URL above._"
          } >> "$GITHUB_STEP_SUMMARY"
